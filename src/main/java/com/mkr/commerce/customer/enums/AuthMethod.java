package com.mkr.commerce.customer.enums;

public enum AuthMethod {
    GMAIL,    // registered via Google OAuth
    MOBILE,   // registered via phone number + OTP
    USER_ID   // registered via email + password
}
