package com.mkr.commerce.catalog.enums;

public enum MediaType {
    IMAGE_PRIMARY,   // main image — shown on cards and search results
    IMAGE_GALLERY,   // additional angle / lifestyle shots
    IMAGE_SPEC,      // spec sheet / label photos
    VIDEO            // product demo or unboxing
}
